name: Codex Maintainer Review

on:
  workflow_dispatch:
    inputs:
      task:
        description: "Review instruction"
        required: true
        default: "Review this repository for documentation, tests, and security issues."

jobs:
  codex-review:
    runs-on: ubuntu-latest
    if: ${{ secrets.OPENAI_API_KEY != '' }}
    permissions:
      contents: read
      pull-requests: read
      issues: read
    steps:
      - uses: actions/checkout@v4

      - name: Codex review placeholder
        run: |
          echo "Codex review workflow placeholder."
          echo "Task: ${{ inputs.task }}"
          echo "Wire this to the official Codex action after confirming account access and spend policy."
