# Security

Please do not report security issues by opening public issues that include secrets or exploit details.

This project executes Codex CLI commands on local repositories. Keep the server bound to `127.0.0.1` unless you add your own authentication, network controls, and monitoring.

Never commit API keys, Codex auth files, `.env` files, job logs, or local chat/session data.
